# BOOT

1. Confirm the root is `D:\app myfirstmoney` and run `git status --short`.
2. Read `AGENTS.md`, `ROADMAP.md`, `STATUS.md`, `patches/CURRENT_SESSION.md`, the current patch, and only exact target files.
3. Use synthetic/demo data only. Do not request `OPENAI_API_KEY` until the server-side BW2 adapter is ready for live verification.
4. Keep `docs/BUILD_WEEK.md` as the sole competition evidence/checklist source.
5. Run the narrowest relevant verification, then update status/evidence only when the state actually changed.

Pipeline source: `D:\brain\projects\product-pipeline\I006-kids-money\PIPELINE.md`.

